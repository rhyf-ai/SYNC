"use client";
import styled from "styled-components";

export default function ShowTables({ chatId }) {
    return (
        <div>
            <h1>Chat ID: {chatId}</h1>
            hiHi
        </div>
    );
}
